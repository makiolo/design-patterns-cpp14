version: 1.0.{build}

platform:
  - x86
  - x64

configuration:
  - Debug
  - Release

install:
  - cmd: pip install pyyaml
  - cmd: pip install poster

build_script:
  - cmd: npm install

test_script:
  - cmd: npm test
