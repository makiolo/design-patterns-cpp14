language: c
services: docker
os: linux
env:
  # - IMAGE=linux-x86
  - IMAGE=linux-x64
  - IMAGE=windows-x86
  - IMAGE=windows-x64
  # - IMAGE=android-arm
  # - IMAGE=browser-asmjs
script:
  - npm run docker
