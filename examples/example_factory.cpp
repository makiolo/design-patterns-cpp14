#include <iostream>
#include <factory.h>

// Example usage of the factory pattern from design-patterns-cpp14

int main() {
    std::cout << "Example: Using design-patterns-cpp14 library" << std::endl;
    std::cout << "This library provides header-only design patterns for C++14" << std::endl;
    
    // The actual usage would depend on your specific patterns
    // This is a simple example showing how to include and use the library
    
    return 0;
}
